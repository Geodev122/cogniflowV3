// Example Jest test stub - requires setup of test environment and supabase test instance

import { createClient } from '@supabase/supabase-js'

// This test assumes you have a test Supabase project and service role key
// Add instructions in README to configure CI with SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY

describe('Signup & profile creation', () => {
  it('should create a profile row when a new user signs up', async () => {
    // Placeholder: implement with test Supabase project
    expect(true).toBe(true)
  })
})
